# 基础依赖
# spirecomm @ git+ssh://git@github.com/ForgottenArbiter/spirecomm.git # 建议手动 clone 到 external 目录并安装，或者配置好 SSH key 后取消注释
numpy>=1.21.0           # 数学计算

# 可选依赖 (后期方案二/三需要)
# torch>=1.10.0
# gym>=0.21.0
# stable-baselines3>=1.3.0
